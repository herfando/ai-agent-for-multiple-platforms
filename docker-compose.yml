services:
  postgres:
    image: postgres:16

    container_name: ai-agent-postgres

    restart: always

    env_file:
      - .env

    ports:
      - "5434:5432"

    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
